#ifndef ARGUMENTLITERALS_H
#define ARGUMENTLITERALS_H

#include <string>

namespace                         htl   {
namespace                         app   {

const std::string                 ID_GSUM_PARAM     ("gsum-list"    );
const std::string                 ID_GSUM_EXCL_PARAM("gsum-exclude" );

const std::string                 ID_FILE_LOG       ( "log-file"    );
const std::string                 ID_FILE_PID       ( "pid-file"    );
const std::string                 ID_FILE_CFG       ( "config-file" );

const std::string                 EXT_FILE_LOG      (".log"   );
const std::string                 EXT_FILE_PID      (".pid"   );
const std::string                 EXT_FILE_CONF     (".conf"   );

}// namespace                     app
}// namespace                     htl

#endif // ARGUMENTLITERALS_H
